{
  "name": "rim-public-mcp-server",
  "version": "0.1.0",
  "description": "MCP server que expone la API pública de Registro IMEI Multibanda Chile a un agente OpenClaw. Diseñado para clientes B2B (mayoristas/certificadores) que reciben IMEIs de sus clientes finales por foto/texto/Excel y necesitan validar, identificar y verificar el estado de homologación.",
  "private": true,
  "type": "module",
  "bin": {
    "rim-public-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
